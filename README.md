# ファイル解析アプリ

Excel / CSV 形式の計測データを読み込み，**MLX90632** と **MAX30102**
に関する評価指標を自動計算・可視化する Web アプリケーションである．\
研究・実験用途を想定し，**ログファイルあり／なしの両方に対応**しながら，タスク区間ごとの比較や固定時間区間ごとの比較を行えるように設計している．

## 使用技術一覧

## 目次

1.  [概要](#概要)
2.  [主な機能](#主な機能)
3.  [想定する入力ファイル](#想定する入力ファイル)
4.  [Version 3.0 での改善点](#version-30-での改善点)
5.  [ディレクトリ構成](#ディレクトリ構成)
6.  [セットアップ](#セットアップ)
7.  [実行方法](#実行方法)
8.  [設計上の工夫](#設計上の工夫)

## 概要

本アプリは，センサデータと基準データを時間同期し，区間ごとの評価指標を自動算出する研究支援ツールである．

対象機能：

-   **MLX評価**\
    体温真値と MLX90632 の出力値を比較し，MAE・RMSE・平均値を算出する．
-   **MLX修正後評価**\
    補正後 MLX データを再評価する．
-   **MAX評価**\
    ECG，PPG_BPM，PPG_fin_BPM を比較する．
-   **PPG→HR解析**\
    PPG 波形から HR を推定し，窓ごとの推定状態を確認する．
-   **PPG＋ACC解析**\
    PPG と加速度から Motion Artifact
    を評価し，HR利用可能率や精度指標を算出する．
-   **ファイル内容表示**\
    CSV / Excel データを時間軸または行番号で可視化する．

解析結果は PNG 保存できる．

------------------------------------------------------------------------

## 主な機能

### 1．解析モードの切り替え

-   ファイル内容表示
-   MLX評価
-   MAX評価
-   MLX修正後評価
-   PPG→HR解析
-   PPG＋ACC解析

### 2．ログファイルあり／なし対応

-   ログあり：`log.csv` の `Task_Name` と `Timestamp`
    を基準にタスク単位で解析する．
-   ログなし：指定した時間区間で解析する．

### 3．MLX時間軸再構築

`RecvJST` と `SensorElapsed_ms` を利用し，センサ基準の時刻を再構築する．

処理：

    再構築時刻 = RecvJST の初期値 + SensorElapsed_ms

ログありの場合は `Start` から `End` までの範囲を抽出する．

### 4．PPG＋ACC解析

10 秒窓，5 秒オーバーラップで解析し，

-   HR推定状態
-   Motion Artifact
-   HR_Usable
-   PPG と ACC の周波数一致
-   コヒーレンス

を評価する．

### 5．結果保存

解析結果を PNG 保存できる．\
PPG＋ACC解析では窓詳細 CSV を出力できる．

------------------------------------------------------------------------

## 想定する入力ファイル

### MLX評価

-   `body_temperature.csv`
-   `log.csv`（任意）
-   `3-Device_Measurement.xlsx`
-   `MLX_*_Measurement*.csv`

### MLX修正後評価

-   `body_temperature.csv`
-   `mlx_re.csv`
-   `log.csv`

### MAX評価

-   `ecg.csv`
-   `PPG_BPM.csv`
-   `PPG_fin_BPM.csv`
-   `log.csv`

### PPG＋ACC解析

-   PPG Quality CSV
-   加速度を含む PPG データ
-   `log.csv`

### ファイル内容表示

任意の CSV / Excel

------------------------------------------------------------------------

## Version 3.0 での改善点

### 1．PPG＋ACC解析機能追加

-   加速度との周波数比較
-   コヒーレンス解析
-   Motion Artifact判定
-   HR利用可能率評価

を追加した．

### 2．PPG品質情報追加

窓ごとに以下を保存する．

-   Window_Start
-   Window_End
-   Window_Center
-   HR_BPM_Window
-   HR_Status
-   HR_Usable

### 3．時間同期処理改善

センサ内部時間を利用した再構築時刻に対応した．

### 4．ファイル表示改善

-   時間軸表示
-   ログ境界表示
-   表示列切替
-   拡大縮小

に対応した．

------------------------------------------------------------------------

## ディレクトリ構成

``` text
.
├── app.py
├── app.js
├── index.html
├── style.css
├── config.json
├── requirements.txt
└── README.md
```

------------------------------------------------------------------------

## セットアップ

``` bash
pip install -r requirements.txt
```

`app.py` と同じディレクトリに以下を配置する．

-   `index.html`
-   `app.js`
-   `style.css`
-   `config.json`

------------------------------------------------------------------------

## 実行方法

``` bash
python app.py
```

その後，ローカルサーバー経由で `index.html` を開く．

解析手順：

1.  ファイル選択
2.  解析モード選択
3.  必要条件設定
4.  解析実行

------------------------------------------------------------------------

## 設計上の工夫

### 1．柔軟な時刻処理

異なるセンサの時刻形式に対応し，統一した時間軸で解析する．

### 2．ログ基準評価

実験ログを利用し，タスク単位の評価を可能にする．

### 3．品質を考慮した解析

PPG＋ACC解析により，モーションアーチファクトを考慮した評価を可能にする．

### 4．設定とロジックの分離

解析条件を `config.json` に外出しし，再利用性を向上させる．

------------------------------------------------------------------------

## 開発者情報

Name: Takato Ishii

Portfolio: https://takato-ishii.vercel.app/
